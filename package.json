{
  "name": "electron-compile-cache",
  "version": "0.5.0",
  "description": "Base class for compilers in electron-compile, use this to implement your own compiler",
  "main": "lib/compile-cache.js",
  "scripts": {
    "compile": "babel --stage 0 -d lib/ src/ && babel --stage 0 --ignore 'test/fixtures/*' -d test-dist/ test/",
    "prepublish": "npm run compile",
    "test": "npm run compile && mocha test-dist/*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/paulcbetts/electron-compile-cache"
  },
  "keywords": [
    "electron"
  ],
  "author": "Paul Betts <paul@paulbetts.org>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/paulcbetts/electron-compile-cache/issues"
  },
  "homepage": "https://github.com/paulcbetts/electron-compile-cache",
  "dependencies": {
    "lodash": "^3.8.0",
    "mkdirp": "^0.5.0"
  }
}
